{
  "name": "@lvce-editor/pretty-error-monorepo",
  "version": "0.0.0-dev",
  "description": "",
  "license": "MIT",
  "author": "Lvce Editor",
  "type": "module",
  "scripts": {
    "#lint": "eslint .",
    "build": "node packages/build/src/build.js",
    "format": "prettier --write .",
    "lint": "echo ok",
    "postinstall": "npm ci --prefix packages/build --ignore-scripts && npm ci --prefix packages/memory --ignore-scripts && npm ci --prefix packages/pretty-error --ignore-scripts",
    "test": "npm test --prefix packages/pretty-error",
    "type-check": "tsc -b"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true
  },
  "devDependencies": {
    "@lerna/legacy-package-management": "^8.2.4",
    "@lvce-editor/eslint-config": "^16.3.0",
    "eslint": "^10.7.0",
    "lerna": "^8.2.4",
    "prettier": "^3.6.2",
    "typescript": "^6.0.0"
  }
}
